var path = require('path');

const config = {
    server: 'localhost',
    database: {
        name: 'syndicator',
        username: 'root',
        password: 'root'
    }
};

module.exports = config;
